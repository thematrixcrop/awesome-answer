{
  "github.com/apache/answer-plugins/connector-wallet": "The frontend build fails because a transitive Coinbase Wallet dependency uses import-attributes syntax that Answer's frontend toolchain cannot parse. Remove this entry after the dependency is compatible with the toolchain.",
  "github.com/apache/answer-plugins/user-center-slack": "The upstream main branch currently fails with undefined: resty in notification.go:113. Remove this entry after the plugin builds successfully."
}
